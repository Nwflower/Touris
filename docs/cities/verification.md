# 城市接入验证记录（历史）

**这是一份历史记录，不是待办清单。** 两次接入验证都发生在 2026-09-12，
各自的团队基线（`aa1ed29`、`8267328`）都已合入 main，记载的工作早已完成。

> ⚠️ **两份记录里关于地图的结论已被推翻。** 当时用的是在线 OSM 瓦片，并明确
> 「不提供离线下载或批量预取」；现在是**构建期预烤的离线栅格瓦片**
> （`prototype/tiles-raster/`），不访问任何外部服务。选型经过见
> `prototype/tiles-raster/CREDITS.md` 与 `prototype/real-maps.js` 的文件头。
> 当前口径以顶层 `README.md` 和本目录 `README.md` 为准。
>
> 同样已过期的还有：「动态天数和真实地图仅扩展杭州、广州，其他城市保留团队原行为」
> （现为六城统一）、以及「`npm test` 团队 131 项模块化检查」（该套测试随模块化重构下线，
> 现为 `npm verify` / `npm run imgcheck`）。

## 一、多天数与真实地图接入（基线 8267328）

### 自动检查

- `cd prototype && node _verify.js`：通过。覆盖京都及 `CITY_DATA` 城市的基础数据和页面回归；
  杭州 18 点、广州 21 点的图片文件与带来源经纬度齐全。
- 两城 × 三主题 × 2—7 天，共 36 组行程；默认路线每天非空、同路线无重复景点；
  2—4 日主题两两交集不超过较大路线点数的 70%。
- 方案、详情与对照天数一致，详情跟随所选主题；跨年日期正确；
  减少赶场偏好不减少天数，关闭记忆恢复路线；不匹配偏好不伪造差异。
- `node --check`：`app.js`、`city-expansion.js`、`real-maps.js` 通过；`git diff --check` 通过。
- `./sync-pages.sh --check`：成功列出待发布的新地图 / 图片 / 脚本，没有发布或推送 gh-pages；
  根目录副本按团队新约定移除。

### 浏览器与照片

- 杭州 4 天：首页入口、方案、详情、对照天数一致；第 4 天地图显示拱宸桥和香积寺。
- 广州 4 天：需求表单选择 4 天后显示 4 天；7 天文博方案从 2026-12-29 到 01-04，
  详情和对照均包含第 7 天；当天地图对应北京路与石室圣心大教堂。
- 两城三卡底图加载；Leaflet 版权入口可见。点击雷峰塔标记显示对应名称；放大按钮有效；
  拖动后地图 pane 位移 45px/-35px。按天配色、切日位置和标记编号对应。
- 两城卡片照片成功加载；原有与新增共 40 张图片通过联系表人工核对；
  新增图片署名在攻略区域及景点详情显示。
- 合入队友代码后重新加载首页，确认六城卡片、本地图片、真实地图和图片许可入口正常。

### 当时声明的范围与限制

按公开资料编排的 MVP 参考行程，不含实时开放、票价或交通导航。地图点位为景点参考位置；
虚线仅表示访问顺序，不是实际道路导航，不生成未经核验的交通耗时。
**（地图部分已失效，见文首说明。）**

## 二、基础接入验证（基线 aa1ed29）

基线包含「唯一开发目录与发布同步约定」。

- `node --check prototype/app.js`、`node --check prototype/city-hangzhou-guangzhou.js`：通过。
- `cd prototype && node _verify.js`：通过。覆盖五城核心页面、两城六套路线、跨年日期、
  两日对照、偏好匹配和图片文件引用。
  同时修复了旧校验脚本未加载 `account.js`、仍使用过时 persona 字段的问题，
  使有记忆 / 无记忆场景真正不同。
- 浏览器：杭州首页快捷入口、古寺文博型选中及详情；6 个景点照片加载成功。
- 浏览器：从需求页切换广州，选择 5 天时按提示生成固定 2 天；2026-12-31 出发显示 12-31、01-01。
- 浏览器：广州 6 个景点照片加载成功，展开来源可见官方链接及 3 篇小红书标题、作者和摘要。
- 浏览器：广州第 1 天「不喜欢 → 太赶」产生一条真实反馈，沙面移为备选；
  对照显示 2 天与 1 处变化；关闭记忆恢复沙面。
- 浏览器：地图按天颜色与连线可见。检查时无 JavaScript error 日志。
- `./sync-pages.sh` 后 `./sync-pages.sh --check`：所有发布文件一致。
- `git diff --check`：通过。

未接入实时地图、交通、票务或第三方攻略 API。原文链接的登录要求和时效限制详见来源清单；
不是所有历史文中的旅行信息都已重新核验。
