# 接入验证记录

验证日期：2026-09-12。基于主分支 `aa1ed29`（包含唯一开发目录与发布同步约定）完成。

- `node --check prototype/app.js`、`node --check prototype/city-hangzhou-guangzhou.js`：通过。
- `cd prototype && node _verify.js`：通过。覆盖五城核心页面、两城六套路线、跨年日期、两日对照、偏好匹配和图片文件引用。修复旧校验脚本未加载 account.js、仍使用过时 persona 字段的问题，使有记忆/无记忆场景真正不同。
- 浏览器：杭州首页快捷入口、古寺文博型选中及详情；杭州 6 个景点照片加载成功。
- 浏览器：从需求页切换广州，选择 5 天时按提示生成固定 2 天；2026-12-31 出发显示 12-31、01-01。
- 浏览器：广州 6 个景点照片加载成功，展开来源可见官方链接及 3 篇小红书标题、作者和摘要。
- 浏览器：广州第 1 天“不喜欢 → 太赶”产生一条真实反馈，沙面移为备选；对照显示 2 天与 1 处变化；关闭记忆恢复沙面。
- 浏览器：地图按天颜色与连线可见。检查时无 JavaScript error 日志。
- `./sync-pages.sh` 后 `./sync-pages.sh --check`：所有发布文件一致；根目录 HTTP 预览再次验证两城入口和本地图片。
- `git diff --check`：通过。

未接入实时地图、交通、票务或第三方攻略 API。原文链接的登录要求和时效限制详见来源清单；不是所有历史文中的旅行信息都已重新核验。
