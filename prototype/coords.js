/* ==========================================================================
   Touris 知途 · 景点经纬度（WGS-84，[经度, 纬度]）

   用途：离线地图（maplibre + PMTiles）打点与画线。
   data.js / city-data.js 里的 x/y 是 0–100 归一化手绘值，只服务于旧的 SVG 示意图，
   两者并存：SVG 用于 S1 方案卡的迷你概览，真实地图用于 S2 详情页。

   数据来源见 coordinate-sources.md；未查证到的一律留空，地图上不画，
   宁可不显示，也不把点打错地方。
   ========================================================================== */
const COORD = {
  京都: {
    // 由地理查证填充
  },
  北京: {},
  上海: {},
  杭州: {},
  广州: {}
};
