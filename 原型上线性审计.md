# 原型上线性审计

> 2026-09-12 · 对象为 `prototype/` 全部 12 个文件
> 问题：**原型里哪些东西根本进不了真实前端？**
> 判据：① 上线后会不会挂 ② 会不会被打穿 ③ 加一个城市/换一个目的地要不要重写

---

## 结论

原型的 **UI 层是资产**——信息架构、记忆标签体系、diff 叙事、S0→S1→S2→S5 动线都是成品，重做要花掉最贵的时间。

进不了前端的是**UI 底下的三层**：外部依赖（3 个域名里 2 个不可控）、内容真实性（评分与来源是编的）、架构（没有取数层，两套数据协议并存）。

按「必须先拆」排序：**4 处硬阻塞 + 6 处上线即暴露 + 3 处规模上不去**。

---

## A. 硬阻塞：不拆就上不了线

### A1. `HOME_IMG` 指向 Trae IDE 的私有生图接口 ★最急

```js
// app.js:277
const HOME_IMG = (prompt, size='landscape_16_9') =>
  `https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=${encodeURIComponent(prompt)}&image_size=${size}`;
```

| | |
|---|---|
| 用在哪 | 3 张 hero 轮播图（`HOME_HERO_IMAGES`）+ 6 张目的地卡图（`DEST_CARDS`），**全在首页第一屏** |
| 为什么不能上 | `trae-api-cn.mchost.guru` 是 Trae IDE 的内部端点：无文档、无 SLA、无鉴权、无配额保证，随时可下线。路由带 `prompt` 明文，还可能被缓存/索引 |
| 更严重的问题 | 这是 **AI 生成的图，却当作京都实景照片展示**。产品的核心卖点是「可核验、可溯源」——用生成的假照片讲真实，一旦被看出来，整条叙事垮掉 |
| 与既定口径冲突 | 你此前明确要求过：不要自造占位物，图片走稳定 API |

**处理**：从首页撤掉，或换成有明确授权来源的真实照片。不能靠它兜底。

### A2. `wsrv.nl` 免费图片代理

```js
// app.js:164-173
/* 本机直连 upload.wikimedia.org 不通（整个 wikipedia.org 都不通） */
const USE_PROXY = true;
'https://wsrv.nl/?url=' + encodeURIComponent(...) + '&w=960&output=jpg'
```

免费第三方代理，无 key、无配额承诺。代理挂掉 = 全部景点图同时挂。同时它意味着**所有图片请求都经过一个第三方**，说不清数据流向。

注意注释本身已经承认了真实处境：**默认网络环境下 `wikipedia.org` 不通**。这不是偶发，是常态。

### A3. 前端伪鉴权

```js
// account.js:1-18 文件头的自述
「密码哈希」只是一层障眼法：任何人打开 DevTools 都能读到 localStorage 里的全部内容，
也能直接在控制台调 login() 冒充任意账号。
```

- FNV-1a + 客户端盐，**挡不住任何人**
- `demo/demo1234` 明文写在源码里（`account.js:24`）
- 记忆全在 `localStorage['zt.v1']`，换设备不跟过去
- 而产品核心话术恰恰是「这些记忆换个 App 带不走」——**现在换个浏览器就带不走，是自己打自己**

### A4. 假评分 + 假来源，且界面上标着真来源

```js
// data.js / city-data.js 里 60 处
{ score:4.7, count:12840, src:'大众点评' }
{ src:'Tabelog 转译' }   // 8 处
{ src:'Booking 转译' }   // 2 处
```

- 全仓库 `data.js` 里 `http` 出现 **0 次**——没有任何一处数据来自真实抓取
- 界面上渲染成 `4.7 分 · 12840 条点评 · 大众点评`，并做成「来源链接样式」（`app.js` 的 `shortSrc`）
- 设计方案 §S2 原话：「可核验性既满足赛道要求，也是**讲给评委的反幻觉设计**」
- **现在这套数字就是幻觉本身。** 这是全场最容易被打穿的一处：评委问一句「这 12840 条哪来的」就结束了
- `'Tabelog 转译'`、`'Booking 转译'` 里的「转译」二字，等于自认是二手编的

---

## B. 上线即暴露

### B1. 首页 6 张目的地卡，有 5 张点进去是错的

```js
const DEST_CARDS = [京都, 奈良, 箱根, 大阪, 东京, 富士山]        // app.js:286
globalThis.CITY_DATA = {北京: beijing, 上海: shanghai};          // city-data.js:210
```

- `CITY_DATA` 里**没有奈良、箱根、大阪、东京、富士山**，也没有京都
- 点这些卡 → `S.req.dest = '奈良'` → `city()` 返回 `null` → 全部访问器静默退回京都全局数据
- 结果：**挂着「奈良」的标题，显示京都的景点**（`app.js:59-66` 的 8 个三元表达式）
- 同时推荐的城市集合（奈良/箱根/大阪/东京/富士山）和你规划的（京都/北京/上海/成都）**完全不重合**

### B2. 地图：四座城市共用同一张手绘底图

```js
function mapBase(labels){ ... }   // app.js:203，硬编码几何
```

- 所有城市共用同一组 `<rect>`/`<path>` 街区形状，**只有 `<text>` 标签按 `mapLabels` 变**
- 京都的「岚山 / 金阁寺周边 / 市中心 / 东山 / 伏见」和北京的「海淀 / 老城 / 朝阳 / 南城 / 西山」画在**完全相同的街区轮廓**上
- 比例尺写死 `约 2km`（`app.js:249, 272`）
- POI 坐标是 0–100 归一化手绘值，不是经纬度 → 接真实地图要**全部重标**
- 这是设计方案 §4 的 **P0 组件**「天时间轴 + 地图点位」，现在是个装饰

### B3. SVG 占位插画当兜底

```js
// app.js:176-189
底层始终是 SVG 占位插画，有联网图就盖在上面
<span class="ph-mark">示意</span>
```

手绘的 temple / river / market / station 几款插画，`spotThumb()` 永远先铺一层。你此前明确说过不要自制占位图。

### B4. 日期写死，且已经开始腐烂

```js
date:'10-02' … '10-05'                    // 京/沪行程，无年份
S.req = { dest, date:'2026-10-02', ... }  // app.js:568 目的地卡写死
$('home-date')?.value || '2026-10-02'     // app.js:558 搜索框兜底写死
```

首页有日期输入框，但 **S1/S2 的日程日期不跟随输入**——`date` 只影响文案，不影响行程。

---

## C. 规模上不去

### C1. 没有取数层 —— 一处 `fetch` 都插不进去

- 数据是**全局 `const`**（`data.js` 的 `POI/SPOTS/MEMORIES/PLANS_*/ITIN_*/DIFFS`、`city-data.js` 的 `CITY_DATA`），页面加载即求值完毕
- 视图直接读全局：`plans()` / `itin()` / `diffs()` / `spots()`（`app.js:64-66`）
- 后端就位后要改的是**每一个视图函数**，而不是一个入口

### C2. 京都走遗留全局，北京/上海走 `CITY_DATA`

```js
function city(){ return CITY_DATA[key] || null; }          // 京都 → null
function poi(){ return city() ? city().poi : POI; }        // → 退回全局 POI
function plans(){ const c=city(); return memActive() ? (c?c.plansMemory:PLANS_MEMORY) : …; }
```

- `city-data.js:1` 的注释自己写着「京都保留在 `data.js`；本文件用同一数据协议扩展城市」
- 于是 **8 个访问器全是三元表达式，两套数据协议长期并存**
- 加城市的成本：要么改 `data.js` 全局，要么改 `city-data.js`，**取决于加的是不是京都**

### C3. 「用了记忆」和「没用记忆」是两份手写数据

| 文件 | 手写几份 |
|---|---|
| `data.js` | `PLANS_DEFAULT` / `PLANS_MEMORY`、`ITIN_DEFAULT` / `ITIN_MEMORY`、`STAY_DEFAULT` / `STAY_MEMORY`、`DIFFS`（7 条） |
| `city-data.js` | 每座城市各一套 `plansDefault` / `plansMemory` / `itinDefault` / `itinMemory` / `diffs` |

- 改一条偏心得同步改多处
- `DIFFS` 里 7 条描述（`app.js:444`）是**手写的字符串**，和实际行程数据可以不一致，**而且没有任何机制会发现**
- 这正是 `app/memory/derive.js` 已经解决的问题（一份默认路线 + 按记忆语义现推）

### C4. 单文件 1792 行 + 全局状态 + 35 分支事件分派

- `app.js` 一文件装下：渲染、交互、地图、首页、账号面板、hero 轮播
- 全部状态在全局 `S` 上，事件靠 `data-act` 字符串分派（`app.js:1544` 起一长串 `if`）
- 无构建、无模块。设计方案里的 S3（版本化编辑）/ S4（记忆中心）/ S6（已保存行程）**整屏没有**

---

## D. 可以保留的（别动）

- **信息架构与屏次动线** S0 → S1 → S2 → S5，设计方案 §5 的 Demo 动线能完整跑通
- **🧠 记忆标签体系**（`memTag` + `memPop` 溯源浮卡）—— 全站复用的差异化核心，已成型
- **元素级表态控件**（👍/👎 + 原因标签 → 学到记忆）—— 设计方案点名「这条直接影响 Demo 成败」
- **diff 播放动画**（`playDiff`）与 **F 键全屏演示模式**（`app.js:1754`）
- **三方案四维标尺**（`planCard` + `paceDots` / `walkBars`）
- **预置 20 条记忆**（作为 Demo 种子是合理的，`account.js` 的惰性播种也写对了）
- 样式层 5 个 CSS 文件

---

## 处理顺序

| 序 | 事项 | 类型 | 理由 |
|---|---|---|---|
| 1 | 撤掉 `HOME_IMG`（Trae 生图） | 删 | 首页第一屏，不可控 + 假照片，改起来最快 |
| 2 | 假评分/假来源：要么接真实 API，要么界面上不再声称来源 | 决策 | 信誉问题，评委一问就穿 |
| 3 | 首页目的地卡对齐真实城市集合 | 改数据 | 5/6 张卡点进去是错的 |
| 4 | 加一层取数边界（`API.*`，后端缺失时回落本地 const） | 加 | 后端一上就切换，不用改视图 |
| 5 | 京都并入 `CITY_DATA`，消掉双协议 | 重构 | 加成都的前置条件 |
| 6 | `DIFFS` 改成推导 | 重构 | `app/memory/derive.js` 已有现成算法 |
| 7 | 地图换真实底图 | 换 | P0 组件，现在是装饰 |
| 8 | 会话/记忆搬到后端 | 加 | 「换 App 带不走」这话术的前提 |

**1–4 是与 `app/` 那条轨道的分水岭**：这四件做完，原型就已经是一个「能接后端的前端」；不做，两条轨道会在同一件事上重复劳动。
