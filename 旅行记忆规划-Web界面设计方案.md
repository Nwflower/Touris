# 旅行记忆规划 · Web 端界面设计方案

> 基于功能模块表（10 个模块）的界面与交互设计方案。
> 总纲：**这个产品的护城河是"记忆"，界面的第一要务就是让记忆可见、可溯源、可对比。**
> 用户和评委都应该在 10 秒内看见"这次推荐和携程/通用大模型有什么不一样"。

---

## 0. 模块设计评审（设计前提）

你们的功能表已经解掉了旅行题材的两个死穴：

- 「出行需求不询问职业、预算」→ 偏好必须从行为中推断，过了"问卷测试"（说得出的偏好不值钱）；
- 「喜欢/不喜欢 + 记忆管理 + 再次推荐对照」→ 构成完整记忆闭环，过了"积累测试"。

因此界面设计不需要补功能，只需要把已有设计的差异化**演出来**。

---

## 1. 设计原则

1. **记忆前置**：记忆不是设置页里的二级功能，而是每一个推荐界面上的可见图层。
2. **对照即 Demo**：「开记忆 / 关记忆」的一键对照是产品叙事的胜负手，按最高优先级设计。
3. **反馈低摩擦、高颗粒**：元素级表态（景点/某天/餐厅），不只对整套攻略表态。
4. **可核验**：餐厅评分、点评摘要必须带来源，满足赛道"可解释、可核验、可修正"的共同要求。
5. **一天可实现**：P0 五屏走通完整 Demo 动线，其余降级。

---

## 2. 信息架构（桌面端三栏）

```
┌──────────────────────────────────────────────────────────┐
│ 顶栏: 产品名 | 当前记忆档案(人设切换) | 记忆开关 | 已保存行程 │
├──────────┬───────────────────────────────┬───────────────┤
│ 左栏(窄)  │                               │ 右栏(可折叠)   │
│ ·出行需求  │        主工作区                │  记忆侧栏      │
│  摘要卡    │  (随流程切换 S0→S1→S2→S5)     │  本次推荐实际  │
│ ·记忆计数  │                               │  用到的记忆    │
│  "已积累   │                               │  逐条列出,     │
│   23条"   │                               │  可跳转溯源    │
│ ·入口导航  │                               │               │
└──────────┴───────────────────────────────┴───────────────┘
```

- **不要**做成"十个模块十个页面"的列表跳转式导航，会散。
- 右栏「记忆侧栏」常驻，展示**本次推荐实际引用到的记忆**——差异化不能埋进"记忆管理"页面。
- 顶栏的「记忆档案切换」既是产品功能（多人共用），也是 Demo 时的人设切换器。

---

## 3. 关键屏幕

### S0 首页 / 出行需求

- 只有 4 个字段：目的地、出发日期、游玩天数、人数。
- 表单下方一行话术点明理念：
  > "我们不问预算和偏好——你的选择历史会告诉我们。"
- 记忆计数徽标："已积累 N 条记忆 · 本次推荐将使用"（空记忆时显示"从零开始，边选边学"）。
- 「旅行记忆初始化」入口放在次要位置（"也可以先告诉我你的过往旅行 →"），**"跳过、从选择中积累"必须是同等显眼的默认路径**——Demo 从空记忆开始现场积累，比预填更有说服力。

### S1 三方案对比（首次路线推荐）

- **最大的坑：三段长文并排，没人读。** 必须结构化。
- 三卡片并排，每卡顶部是**风格画像标签**：
  - 例：「暴走打卡型」「慢逛本地型」「轻松度假型」
- 卡片中部用**统一标尺**可视化四个维度，差异一眼可比：

  | 维度 | 可视化方式 |
  |---|---|
  | 行程节奏 | 每日步数条 / 节奏刻度（松——●——紧） |
  | 景点密度 | 每日景点数 |
  | 住宿范围 | 区域名 + 与核心区距离 |
  | 餐饮策略 | 标签（网红店 / 本地小馆 / 市集） |

- **记忆影响标注（核心组件，全站复用）**：任何受记忆影响的元素挂小标签
  > 🧠 因为你 3 月在大阪给"早起逛黑门市场"点了喜欢
  悬停/点击展开来源记忆详情。
- 空记忆状态下所有方案无 🧠 标签——这本身就是 Demo 的第一幕："看，现在它和通用工具没有区别。"
- 表态语义（对应"未选择≠不喜欢"）：
  - 被选中的方案：✓ 已选择
  - 其余方案：默认中性「跳过」，**只有显式点踩才记为负反馈**
  - 界面上必须让"跳过"和"不喜欢"是两种不同的视觉状态。

### S2 攻略详情

- 按天**时间轴**布局 + 地图联动（当日点位连线）。
- 每天卡片内：景点顺序、住宿范围、餐厅卡。
- 餐厅卡：评分 + 点评摘要 + **来源链接样式**（如"大众点评 · 4.6 · 摘要来自 128 条评价"）——可核验性既满足赛道要求，也是讲给评委的反幻觉设计。
- **元素级喜欢/不喜欢**（关键决策）：每个景点、每天节奏、每家餐厅都可单独表态。
  - 交互：👍/👎 + 快捷原因标签（太赶 / 太远 / 我晕博物馆 / 想吃这个 / 不想早起 …）+ 可折叠自由文本。
  - 若只能对整套攻略表态，记忆颗粒度太粗，第二次推荐的"差异"演不出来——这条直接影响 Demo 成败。
- 每个被记忆影响的安排同样挂 🧠 溯源标签。

### S3 修改当前攻略 / 围绕景点重新规划

- 编辑器：时间轴上**拖拽**增删景点；删除即时，添加用搜索框。
- 「特别想去」星标：星标景点成为必含约束，点「围绕它重新规划」生成新版本。
- **版本页签**：v1 / v2 / v3 顶部可回切对照，旧版本永远保留（对应"保留旧版本"模块）。
- 路线合理性检查做成**警告条而非阻断**：
  > ⚠ 第 2 天「清水寺」与「伏见稻荷」相距 38 km，建议调整顺序或拆到两天。
- 每次修改后提示："这次修改已记入你的旅行偏好（可改）"——把编辑行为也变成记忆来源。

### S4 记忆中心

- 每条记忆一张卡：
  ```
  ┌─────────────────────────────────────┐
  │ "不喜欢早起赶路"                      │
  │ 来源: 2026-03 大阪攻略 · 你对第2天    │
  │       节奏点了不喜欢 ("太早出门")     │
  │ 作用域: [仅本次] [长期 ●]            │
  │ 被引用: 3 次推荐                     │
  │                    [编辑] [删除]      │
  └─────────────────────────────────────┘
  ```
- 三要素必须齐全：**内容、来源、作用域**。来源可点击跳回产生它的那次选择。
- 「仅本次 / 长期」做成显眼的双态开关——这是你们模块表里的加分设计，别藏起来。
- 顶部支持按旅程 / 按类型筛选。

### S5 再次推荐与对照（★ 全场最重要的一屏）

- 顶部居中一个大号开关：**「使用记忆」/「默认推荐」**。
- 切换时播放 **diff 高亮动画**：发生变化的安排逐个亮起，顶部出现统计条：
  > 「记忆改变了本次 7 处安排：砍掉 2 个博物馆、住宿从心斋桥换到天王寺、新增 1 个早市…」
- 每处变化挂 🧠 溯源标签，点击直达对应记忆卡。
- 建议本屏支持**全屏演示模式**（隐藏左右栏），一屏演完赛道那句"同一套系统，喂进去的人不同，产出完全不同"。
- 进阶（P1）：左右分屏并列"有记忆的你 vs 无记忆默认"，同步滚动。

### S6 已保存行程

- 简单列表即可：旅程卡 + 版本数 + 回看入口。P2，最后做。

---

## 4. 关键组件清单（开发视角）

| 组件 | 用途 | 优先级 |
|---|---|---|
| 🧠 记忆溯源标签（含悬停卡片） | 全站复用，差异化核心 | P0 |
| 记忆开关 + diff 高亮 | S5 对照 | P0 |
| 元素级表态控件（👍👎+原因标签） | S1/S2 | P0 |
| 方案对比标尺（节奏/密度/住宿/餐饮） | S1 | P0 |
| 天时间轴 + 地图点位 | S2/S3 | P0 |
| 记忆卡（内容/来源/作用域） | 右栏 + S4 | P0(右栏) P1(S4) |
| 版本页签 + 拖拽编辑 | S3 | P1 |
| 人设切换器 | 顶栏，Demo 用 | P0 |

---

## 5. 一天开发优先级

- **P0（必做）**：S0 需求输入 → S1 三方案对比 → S2 详情+元素级反馈 → S5 对照视图 + 记忆标签体系
- **P1**：S4 记忆中心、S3 版本化编辑
- **P2**：S6 保存列表、记忆初始化向导（可跳过的简版）

**Demo 动线（P0 即可讲完整故事）**：

1. 空记忆人设 → 输入"京都 4 天" → 三套通用方案（无 🧠，"现在它和通用工具没区别"）
2. 在详情页做 3-4 次元素级表态（"不喜欢这个博物馆，我晕博物馆""喜欢这家抹茶店"）
3. 切到预置的"有 20 条记忆"人设 → 同样输入"京都 4 天"
4. 第二次推荐明显不同，逐处挂 🧠
5. S5 对照开关一拨：默认 vs 记忆，diff 亮起，"记忆改变了 7 处安排"
6. 收尾话术："这些记忆换个 App 带不走——这就是它抄不走、你离不开的原因。"

---

## 6. 视觉基调

- 暖色旅行感（米白底 + 陶土橙/靛蓝单一强调色），卡片化，充足留白。
- 地图用低饱和度底图，点位用强调色。
- **避免做成又一个"AI 对话框"**：对话式界面会让评委本能拿它和通用大模型比较；要展示的是"结构化记忆资产"，用卡片、时间轴、标尺说话。
- 🧠 标签全站统一色（建议靛蓝），形成"看到这个颜色就知道是记忆在起作用"的条件反射——这也是评委扫一眼就能抓住的视觉钩子。
